# This is /etc/gunicorn.d/vald.conf
# which is used together with the init-script
# from Debian's gunicorn 0.12

CONFIG = {
    'mode': 'django',
    'environment': {
        'PYTHONPATH': '/opt/ValdNode11.05/',
    },
    'working_dir': '/opt/ValdNode11.05/nodes/vald',
    'user': 'vamdc',
    'group': 'vamdc',
    'args': (
        '--bind=unix:/tmp/valdold-gunicorn.sock',
        '--workers=9',
        # '--worker-class=egg:gunicorn#sync',
        '--timeout=60',
        'settings_atom.py',
    ),
}
